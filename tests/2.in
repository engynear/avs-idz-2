#include <stdio.h>#include <string.h>int main() {char str[1000];int i, if_count = 0, do_count = 0, int_count = 0, for_count = 0, else_count = 0;fgets(str, 1000, stdin);for (i = 0; i < strlen(str); i++) {if (str[i] == 'i' && str[i + 1] == 'f') {